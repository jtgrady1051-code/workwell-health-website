# Assets

- `krista-headshot.jpg` — Krista's professional headshot. Drop the photo file here with
  this exact filename and the About page (about.html) will pick it up automatically
  (there's a graceful "KR" monogram fallback shown until the file exists).
